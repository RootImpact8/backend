spring:
  profiles:
    active: default
  datasource:
    url: jdbc:h2:file:./data/mydb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE  # 🔥 파일 모드 사용 (데이터 유지)
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true  # 🔥 H2 콘솔 활성화
      path: /h2-console  # 🔥 H2 콘솔 URL 지정
      settings:
        web-allow-others: true  # 🔥 다른 클라이언트에서도 접속 허용 (보안 주의)
        trace: false  # 🔥 SQL 실행 로그 비활성화
  sql:
    init:
      mode: never  #always 이거는 creqte 할때 같이 바꿔주기 !!!!!!!!!!!!!!
      data-locations: classpath:data.sql
  jpa:

    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update # 🔥 기존 데이터를 유지하며 테이블 변경 가능하도록 설정
      #transaction:
       # jta:
        #  platform: org.hibernate.engine.transaction.jta.platform.internal.NoJtaPlatform
    defer-datasource-initialization: true  # ✅ data.sql을 실행하기 위해 필요
    show-sql: true

  ai:
    openai:
      api-key: #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

      chat:
        options:
          model: gpt-4o
          temperature: 0.7

server:
  error:
    include-message: always  # 🔥 에러 메시지를 상세하게 출력

logging:
  level:
    root: INFO
    org.springframework.security: DEBUG  # 🔥 보안 관련 로그 출력